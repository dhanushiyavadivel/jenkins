pipeline {
     agent any
     stages {
          stage('build') {
               steps {
                   echo 'hello world'
                       echo 'multiple shell stpe works too'
                }
          }
      }
}

